import { ComingSoon } from '@features/coming-soon';

export default function TimelineScreen() {
  return (
    <ComingSoon
      title="Timeline"
      line="Fluxul vieții tale financiare — fiecare Eveniment, intercalat cu insight-uri AI. Inima aplicației."
      phase="Se activează în Faza 1 (Evenimente + Timeline)."
    />
  );
}
